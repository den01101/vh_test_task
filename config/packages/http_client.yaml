framework:
    http_client:
        default_options:
            headers: { Accept: 'application/json' }

        scoped_clients:
            weather.http.client:
                base_uri: '%env(WEATHER_BASE_URL)%'
                query: { key: '%env(WEATHER_API_KEY)%' }
                headers: { Accept: 'application/json' }